<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.project.movieadmin.board.comments">
	<insert id="INSERT" parameterType="com.project.movieadmin.board.comments.CommentsVO">
		insert into
		comments(nickname, content, wdate, good, board_num, report)
		values(#{nickname},#{content},#{wdate},#{good},#{board_num},#{report})
	</insert>

	<update id="UPDATE">
		update comments set nickname=#{nickname},content=#{content}
		where comments_num=#{comments_num}
	</update>

	<delete id="DELETE">
		delete from comments where comments_num=#{comments_num}
	</delete>

	<select id="SELECT_ONE" resultType="com.project.movieadmin.board.comments.CommentsVO">
		select * from comments where comments_num=#{comments_num}
	</select>

	<select id="SELECT_ALL" resultType="com.project.movieadmin.board.comments.CommentsVO">
		select * from comments order by comments_num desc
	</select>

</mapper>